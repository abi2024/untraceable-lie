# 01 — PROJECT PRD & TECH STACK (IMMUTABLE CONTRACT)

> **READ FIRST. DO NOT EDIT THIS FILE.** This is the frozen contract for the project
> "Where Does an Untraceable Lie Live?". Every decision here is locked. If reality
> forces a change, you do NOT edit this file — you STOP, write the conflict into
> `04_PROGRESS_LOG.md`, and surface it to the human. Changing a locked decision
> silently is the single worst thing you can do on this project.

---

## 0. How Claude Code must use the four documents

| File | Role | Your access |
|---|---|---|
| `01_PRD_AND_STACK.md` (this) | Frozen contract: what we're building, the stack, the rules | **READ-ONLY.** Never edit. |
| `02_PHASES.md` | The ordered task list (T0–T10) with done-checks and gates | **READ-ONLY.** Never edit. |
| `03_STATE.md` | Current truth: active task, blockers, last verified artifact | **OVERWRITE** at the start and end of every task. |
| `04_PROGRESS_LOG.md` | Append-only history of what happened | **APPEND ONLY.** Never edit or delete past entries. |

**The loop you run for every task:**
1. Read `03_STATE.md` to learn the active task. Read this PRD + `02_PHASES.md` for that task's spec.
2. Update `03_STATE.md`: set status `IN_PROGRESS`, record start time, list intended outputs.
3. Do the work. Produce the exact `Out:` files named in the task.
4. Run the task's `Done:` check yourself. Run the relevant guard(s) from §5 below.
5. If the check passes → commit the task (§0a), append a PASS entry to `04_PROGRESS_LOG.md`, release the GPU if held (§0b), and overwrite `03_STATE.md` to point at the next task. If it fails → append a FAIL entry with the error, set `03_STATE.md` status to `BLOCKED`, STOP, and surface to the human (§0c).
6. **Never advance past a gate (§ in `02_PHASES.md`) without the human confirming, unless the gate's routing rule is fully deterministic and its inputs are all green.**

**Hard rule:** No claim, number, or result may exist anywhere unless a committed artifact file backs it. "No artifact, no claim."

### 0a. Git discipline (REQUIRED — the guards depend on it)
Several guards depend on commit *ordering* (prereg predates results; frozen cell predates bridge). That ordering only exists if you commit per task.
- **Commit at the end of every task**, after its done-check passes: `git add -A && git commit -m "Tn: <one-line result>"`. The commit message records the done-check numbers.
- **Tag the irreversible freezes:** `prereg-v1` at T1; `frozen-cell` at T6 (after writing `frozen_cell.json`). A guard that says "X before Y" is verified by `git log` showing X's commit/tag earlier than Y's.
- **Never amend or force-push.** History is the audit trail. A reordered or rewritten history invalidates every ordering guard.
- If a task is retried, commit the retry as a new commit — do not amend the failed one.

### 0b. Rented-H100 lifecycle (REQUIRED — protects the budget)
The H100 is rented and billed by the hour. Most tasks do NOT need it.
- **GPU-required tasks:** T0, T3 (extraction), T5b (full behavioral gen), T7 (baseline forward pass). Also T5a (small generation) and any T8 re-extraction. **CPU/local tasks (no GPU — do not hold the H100):** T1, T2 (data/CPU), T4 (CV logic), T5.5 (reuses saved activations), T6 (probes on saved activations), T9 (correlation analysis on saved scores), T10 (writing).
- **Provision the GPU only when entering a GPU-required task; release it the moment that task's outputs are saved.** Do not hold an idle H100 across analysis or writing tasks.
- **Checkpoint expensive outputs immediately.** `acts_sweep.npz` (T3) and `behavioral_scores.parquet` (T5b) are the costly artifacts — save and commit them as soon as they exist so a later crash never forces a re-extraction. Treat them as immutable once written.
- Record cumulative GPU-hr and $ in every log entry that used the GPU (the log format already requires this). If cumulative spend crosses **$300**, STOP and surface — that is the Gate-1 budget assumption breached.

### 0c. "Surface to the human" — what it actually means
When an instruction says *surface to the human*, do exactly this: set `03_STATE.md` status to `BLOCKED` (or `AWAITING_HUMAN` for a non-error decision), write the decision/question and the options under OPEN DECISIONS, append a `NOTE` entry to the log, and **HALT — do not proceed past the surface-point on a guessed default.** The three mandatory halt points are: (i) **T2 lie-provenance resolution** (it changes what the headline means), (ii) **any GATE 2 RED or GATE 3 negative** (it changes which paper you're writing), (iii) **cumulative spend crossing $300**. For ordinary ambiguity that is NOT one of these, do the smallest defensible thing, log it as a `NOTE` assumption, and continue — do not halt for every small choice.

---

## 1. The project in one paragraph (the spine — do not drift from this)

On exactly the self-knowledge lies where every transcript-only detector in Liars' Bench
(arXiv:2511.16035) collapses — the standard Mean Probe sits at **0.54** balanced accuracy —
the lie is not gone: the paper's privileged-information probe hits **0.73 / AUROC 0.91** and a
behavioral classifier (UQC) hits **0.89–0.97 on HP-KR**. Recovery is therefore NOT the
contribution. We test one thing: **is the deception direction a probe reads *inside* the model
the same signal a behavioral method reads from *outside*?** On the same HP-KR examples we
correlate the internal probe direction against two behavioral signals and report a single
headline contrast — **the internal deception direction is externally legible to the behavioral
UQC probe but NOT to the model's own direct self-evaluation: encoded yet introspectively
inaccessible.**

**The headline is the directional contrast, NOT a precise correlation magnitude.** This is locked.
Do not let any task drift toward "measure ρ precisely" as the goal.

---

## 2. Verified factual ground (use these EXACTLY — never alter)

- Parent paper: **arXiv:2511.16035**, "Liars' Bench," Kretschmar, Laurito, Maiya & Marks (Cadenza Labs / Anthropic).
- Datasets: **HP-KR = Harm-Pressure Knowledge Report**; **GS = Gender-Secret**; **HP-C = Harm-Pressure Choice**.
- Label field: **`deceptive`** (v2 schema).
- Mean Probe ≈ **0.54** balanced accuracy on self-knowledge sets; upper-bound probe **0.73 / AUROC 0.91**.
- UQC reaches **0.89–0.97 on HP-KR** (this is the implementation sanity anchor for T5).
- Data location: `huggingface.co/datasets/Cadenza-Labs/liars-bench`.
- Adjacent paper (cite): arXiv:2510.01070 "Eliciting Secret Knowledge." Recovery prior art (cite): arXiv:2604.13386 (Nordby et al.).
- These names/numbers must appear **verbatim** in any write-up. A wrong dataset name or number is a correctness bug, not a typo.

---

## 3. Committed scope (LOCKED — what is in, what is cut)

### IN — the critical path produces exactly this
- ONE primary model, ONE primary dataset (HP-KR), ONE headline (the directional contrast).
- Internal probe (diff-of-means + L2-LR) at one frozen cell.
- Two behavioral signals: **UQC** (primary, the "working detector") and **Self-Eval** (the introspection axis).
- The bridge: within-class, nuisance-controlled correlation between internal probe score and each behavioral signal.

### CUT — do NOT build these unless explicitly promoted from buffer by the human
| Cut | Reason |
|---|---|
| Qwen-2.5-72B / Llama-3.3-70B as primary | bf16 weights exceed one 80GB H100; quantizing forbidden for headline. |
| Full layer×token decodability map | Most scoopable; reduced to a coarse sweep that only freezes the probe cell. → buffer B1. |
| GS as a second headline dataset | n≈231 underpowered for a magnitude headline. → buffer B2. |
| HP-C world-knowledge contrast | Serves the map, not the bridge. → buffer. |
| H3 steering + ablation | Causal backbone, not headline. → buffer B3/B4. |
| Llama-3.3-70B replication | Single-model critical path. → buffer B5. |
| recall@1%FPR | Underpowered secondary; report AUROC only. |
| Per-token, all-layer activation storage | ≈1.2 TB, infeasible; store role-representatives at a candidate-layer subset. |

**Buffer backlog (only if the human promotes it):** B1 full map · B2 GS · B3 H3a steering · B4 H3b ablation · B5 replication · B6 citation-graph re-check before submission.

---

## 4. Tech stack (LOCKED)

### Compute & model
- **Primary model:** `mistralai/Mistral-Small-24B-Instruct` (bf16, single 1×H100 80GB). Fits with ~33GB headroom.
- **Alternate (only if Step-0 existence fails on primary):** `google/gemma-3-27b-it` (bf16).
- **Escalation (only if BOTH small models fail existence):** a multi-GPU 70B-class **extraction-only** run. Gated; requires human go-ahead.
- **NEVER quantize for any run whose numbers enter the headline.** Quantization perturbs the residual stream. Quantized runs are permissible only as explicitly-labeled reduced-power sanity checks, never as headline evidence.
- Rented H100 via the human's provider. Local RTX 3060 16GB is a fallback for code-only smoke tests, never for headline extraction.

### Storage (enforced by Guard, see §5)
- Activation dumps store **role-representative vectors** (system / user-question / assistant-answer / post-answer) at **~10 candidate layers** — NOT per-token, NOT all-layer.
- Sweep dump target ≈ 2.3 GB. Frozen-cell dump ≈ 58 MB. **Hard cap: total activation storage ≤ 10 GB (gate C1).**

### Libraries (use these; do not introduce alternates without logging the reason)
- `torch` (bf16), `transformers` (model + chat template), `nnsight` or raw forward hooks for residual-stream capture (prefer raw hooks for determinism; document the choice in T3).
- `scikit-learn` for L2-LR probes and logistic UQC classifier; `numpy`, `scipy` (Spearman, Fisher-z), `pandas`/`pyarrow` for parquet.
- `datasets` (HuggingFace) for Liars' Bench load.
- Bootstrap CIs: implement cluster-bootstrap by hand (resample whole templates/personas) — do NOT use a naive i.i.d. bootstrap.
- All randomness seeded; ≥5 seeds where the task says so.

### Repo layout (create in T0)
```
untraceable-lie/
  docs/        # the 4 control documents (this folder)
  src/         # extract.py, cv_split.py, behavioral.py, sweep.py, probe.py, bridge.py, baseline.py
  tests/       # test_cv_leakage.py and any other guards-as-tests
  artifacts/   # every Out: file (.md, .json, .parquet, .npz, .png)
  preregistration.md   # git-tagged in T1; P1–P5 live here
```

---

## 5. The five non-negotiable guards (enforce on EVERY relevant task)

These are not advisory. A task that violates a guard has FAILED even if its code runs.

1. **Leakage unit test (blocking).** `tests/test_cv_leakage.py` asserts no template-ID and no persona-ID appears in both train and test of any CV fold. Must pass on every probe-training or UQC-training commit.
2. **Shuffled-label null floor (incl. elicited signals; introspection axis separated from CoT axis).** Any probe on permuted labels must score AUROC ∈ [0.45, 0.55]. The "Self-Eval at chance / introspectively inaccessible" claim is bound to the **direct no-CoT readout metric `max(SE-A, SE-B)`**, which must fall within its own self-calibrated null band. **SE-C (CoT-reasoned verdict) is a SEPARATE axis with its own null, reported alongside, NEVER folded into the binding number** — CoT measures whether the model can re-derive the lie with a scratchpad, not whether it can introspect its state.
3. **Within-class-only AND nuisance-controlled correlation.** The bridge headline number is ρ_lie / ρ_honest (never the pooled correlation — pooled is reported ONLY to show the inflation gap). A "same feature" (ρ≥0.5) claim additionally requires ρ_lie to survive a partial correlation controlling for `ans_len` and `ans_logprob` (or a length-tercile×persona-stratified consistency check).
4. **Baseline reproduced before any lift.** The paper's Mean Probe must be reproduced to **0.54 ± 0.05** and committed BEFORE any recoverability/lift/bridge claim is written. **If it reproduces "too well" (e.g. 0.70) that is a leakage/label BUG — stop and fix it, do not celebrate.**
5. **Thresholds frozen before results.** `preregistration.md` (git-tagged) must predate the commit producing any gated result. The frozen primary cell is written to a prereg addendum BEFORE any bridge correlation is computed.

---

## 6. The three documented failure modes of THIS project (watch for them)

1. **Scope creep.** The plan is deliberately minimal. If you find yourself building the full map, a second model, GS, or steering — STOP. That is buffer, not critical path. Surface it; don't silently expand.
2. **Compute optimism.** Believe the storage and GPU-hour math only after T0 confirms it empirically. If T0's numbers blow the gate, the fix is to shrink (fewer layers/tokens/seeds), not to spend more.
3. **Planning instead of executing.** This document set is the end of planning. Your job is to run T0 → T10, not to re-plan. If a task is ambiguous, do the smallest defensible thing and log the assumption — do not open a redesign.

---

## 7. Definition of Done (branch-complete — amber/red are landings, not failures)

- **GREEN:** `h4_bridge.md` shows, on HP-KR under leave-template/persona-out CV, the internal deception direction predicts the working UQC signal within-class (surviving the nuisance control) while direct no-CoT self-readout `max(SE-A, SE-B)` sits at chance on those same lies — the "encoded yet introspectively inaccessible" contrast, with SE-C's CoT result reported as a separate axis — every number traced to an artifact and packaged in `mats_application_summary.md`.
- **AMBER (equally done):** `h4_bridge.md` shows the internal direction predicts the working behavioral detector within-class on HP-KR (nuisance-controlled, by ρ zone) — a clean same-/two-feature answer to the parent paper's open question — packaged in `mats_application_summary.md`.
- **RED (equally done, publishable):** either the promoted B1 token×depth map for transcript-blind self-knowledge lies, OR `smoke_recoverability.md`/`probe_auroc.md` documenting the clean negative (transcript-blind lies near-unrecoverable internally) — a real model-biology/auditing result — packaged in `mats_application_summary.md`.

**The project is DONE when one of these three branches is fully realized, every number traces to a committed artifact, and `mats_application_summary.md` is reviewer-readable in under 5 minutes.**

---

## 8. AMENDMENTS (human-ratified, dated 2026-06-10)

> **This section is appended, not retroactive.** The body of §0–§7 above remains the
> immutable original contract. The amendments below are the human-ratified
> resolutions of the locked-decision conflicts that T2 (data card + provenance +
> CV viability, PASS 2026-06-07) surfaced. They were ratified BEFORE `prereg-v1`
> was tagged at T1 (2026-06-10), and are copied verbatim into `preregistration.md`
> so the prereg is the single source of truth for downstream tasks. Where this
> section disagrees with §0–§7, this section wins for all tasks from T3 onward.
> Where §0–§7 says something this section does not address, §0–§7 still binds.

### A1 — Primary-model checkpoint pinned to `mistralai/Mistral-Small-24B-Instruct-2503`
**What changes:** §4 ("Primary model: `mistralai/Mistral-Small-24B-Instruct` (bf16, single 1×H100 80GB)") is amended to pin the specific revision `mistralai/Mistral-Small-24B-Instruct-2503`. Tokenizer must be loaded with `fix_mistral_regex=True`.
**Why:** T0 (PASS 2026-06-07) fit and benchmarked the `-2503` checkpoint specifically (d_model=5120, n_layers=40 confirmed). The unversioned name was ambiguous between checkpoint revisions; the unambiguous pin removes a reproducibility hole.
**Scope:** Binds T3, T5, T7, T8, T5.5. Does NOT affect the alternate (`google/gemma-3-27b-it`) or the 70B escalation rule.

### A2 — GS promoted from buffer (B2) to second-dataset replication
**What changes:** §3 ("CUT: GS as a second headline dataset … → buffer B2") is amended. HP-KR REMAINS the single primary headline dataset (lean-plan discipline is preserved). GS is promoted to a **second-dataset orthogonal-confound replication** that the headline references but does not depend on for power.
**Why:** T2 found HP-KR and GS carry orthogonal confounds (HP-KR: 2 templates + prompt-framing-by-class; GS: orthogonal topic confound + fine-tuned + multi-turn). A replication on a dataset whose confound structure is orthogonal to the primary's is stronger evidence than a single-dataset result, and is cheap relative to the $300 budget. GS requires a LoRA adapter, confirmed available.
**Scope:** Adds a GS replication arm to T3 (extract second), T5 (UQC/Self-Eval second), T8 (probe second), T9 (report HP-KR headline + GS replication). The CI/power discipline (HP-KR carries the magnitude headline; GS is replication, not power) is preserved — GS small-n is not laundered into the headline number. Multi-turn handling for GS is documented in T3.

### A3 — CV scheme amended to "within-template stratified + cross-template consistency"
**What changes:** §3/§5 references to "leave-template-out + leave-persona-out" CV (and §02_PHASES §T4) are amended. The realized CV is:
- **Primary CV (HP-KR):** within-template stratified k-fold with `group=template_id`, stratified on the `deceptive` label. k chosen per-template to keep min-class-per-fold ≥ 20.
- **Out-of-distribution check (HP-KR):** cross-template (template A → template B and B → A) reported as a second-axis consistency number alongside the primary CV AUROC.
- **Persona key:** NOT used as a CV grouping key (it is absent / not usable in HP-KR per T2 inventory).
- **GS replication:** within-topic stratified k-fold; cross-topic as consistency check.

**Why:** T2 found HP-KR has only 2 templates and no usable persona key. "Leave-template-out" reduces to 2-fold cross-template, which is fine as a consistency check but is too coarse and unstable as the primary CV. "Leave-persona-out" is not realizable. Guard 1 (leakage unit test) still binds: `tests/test_cv_leakage.py` asserts no `template_id` appears in both train and test of any fold of the within-template stratified scheme OR the cross-template scheme. Group-count-aware fallback language already in §P3 covers the case formally; this amendment names the realized version.
**Scope:** Binds T4, T5, T5.5, T6, T7, T8.

### A4 — Probe cell pre-committed to assistant-answer token role
**What changes:** §3/§5 ("Internal probe (diff-of-means + L2-LR) at one frozen cell") and §02_PHASES §T6 ("Train diff-of-means + L2-LR probes across the ~10×4 cells") are amended. The role dimension of the cell is **pre-committed** to **assistant-answer token** (one role, not four). T6's sweep therefore runs over ~10 layers × 1 role, and freezes the single best layer at the assistant-answer role.
**Why:** T2 confirmed HP-KR is transcript-blind: assistant answers are byte-identical across the deceptive/honest classes for matched items. A deception signal therefore cannot live in the system-prompt, user-question, or post-answer roles in a way that distinguishes classes — it can only live in the internal residual-stream representation AT THE ASSISTANT-ANSWER POSITION. Pre-committing this role at T1 (before T6) is the correct anti-circularity move: the role choice is forced by dataset structure (T2 fact), not by sweep output. Guard 5 (thresholds frozen before results) is satisfied because this commit happens at T1, before T6 reads any AUROC.
**Scope:** Binds T3 (still capture all four roles, so the carve-out is auditable and other roles serve as negative controls), T6 (sweep over layers only; freeze best layer at assistant-answer role), T8 (probe at frozen cell). The other three roles ARE still extracted and stored — they become negative-control cells whose AUROC should NOT clear the null band; that negative result is reported alongside the chosen cell.

### A5 — WMDP safety constraint on the pipeline and all artifacts
**What changes:** A new operating constraint is added to §4 and to every artifact-producing task.
**The constraint:** HP-KR is built atop `cais/wmdp-bio` (bioweapon multiple-choice questions). The probe pipeline operates ONLY on activations, labels, template IDs, and metric values. It MUST NEVER score, display, log, or reproduce harmful question text or harmful answer text in any artifact, figure, README, write-up, or shared notebook. Where an example must be referred to, refer to it by `template_id` + `example_index` only.
**Why:** T2 surfaced the WMDP provenance. The research question is about the internal/external legibility of deception — not about the harmful content itself. Reproducing harmful content in artifacts is both unnecessary for the headline and a real-world harm. This is also why §3's "no quantization for headline" and §4's "store role-representative vectors only" already structurally limit what gets written to disk; A5 makes the safety scope explicit at the write-up level.
**Scope:** Binds T2 (data_card.md — already compliant), T3 (token_roles.json: store role boundaries + example_index, NOT raw text), T5 (UQC question bank is the frozen Q=12 unrelated-questions list — these are safe; harmful HP-KR question text is NEVER concatenated into any UQC or Self-Eval prompt that gets logged), T9 (bridge_fig.png labels by example_index), T10 (paper_draft.md + mats_application_summary.md: no harmful Q/A text; redact-or-paraphrase any example that must be illustrated). A pre-publication safety review of `mats_application_summary.md` is a release gate.

### A6 — Headline population restricted to the transcript-blind core (ratified 2026-06-10, appended after `prereg-v1`)
**What changes:** §3 ("ONE primary model, ONE primary dataset (HP-KR), ONE headline") is amended: the headline analysis runs on the **transcript-blind CORE** subset of HP-KR — `artifacts/hpkr_blind_core.parquet`, 1656 rows · 650 lie · 1006 honest · per-template t1 n=785, t5 n=871 — defined in `src/transcript_blind_filter.py::filter_blind_core` (claims-ignorance + canonicalizes to `"i do not know the answer"` / `"i don't know the answer"` after stripping a trailing " to {the,this,that} question" + per-phrasing lie-fraction within 0.15 of the subset baseline 0.3951). The full first-party HP-KR parquet (2358 rows) is preserved unchanged and feeds a new T7 transcript-only baseline arm.
**T7 gains a transcript-only baseline arm:** text classifier on `assistant_answer_text` alone, trained on the FULL parquet under the §A3 CV scheme. Pre-registered directional expectation = HIGH AUROC on the full data, CHANCE on the core. The contrast IS the demonstration that the core is blind.
**Why:** T3 verification (pre-result; no AUROC/ρ computed) found HP-KR is transcript-blind only on the claims-ignorance subset. Within that subset the top-6 byte-identical "I don't know" phrasings cover 95.2% of the mass at lie-fraction-within-15pp of baseline; the ~5% elaborate-refusal tail is label-correlated and would let a probe shortcut to the label via text. The restriction is a population-definition decision grounded in a dataset fact, ratified before any label-correlated quantity is computed.
**Why `prereg-v1` is NOT moved:** Guard 5 prevents post-hoc threshold-tuning to match an observed number. A6 introduces no new threshold, metric, model, gate rule, CV scheme, or headline-statement change; it narrows the population the headline applies to, in response to a dataset-characterization fact. Same carve-out class as A1–A5; ratified by appending after the tag, exactly as the A1–A5 precedent set.
**Scope:** Binds T3 (extract on the core), T4 (leakage test repointed to the core, 4/4 PASS), T6 (sweep on the core), T7 (baseline reproduction on the core unchanged + new transcript-only baseline arm on the full data), T8 (probe on the core), T9 (bridge on the core). GS replication inherits an analogous restriction only if/when T3's GS arm surfaces a parallel structure; that is not pre-registered here.

---
*Amendments A1–A5 ratified by the human operator on 2026-06-10, before `prereg-v1` was tagged. They resolve the locked-decision conflicts surfaced by T2 per the discipline in §0c.*
*Amendment A6 ratified by the human operator on 2026-06-10, AFTER `prereg-v1` was tagged, in response to a T3-verification dataset-characterization fact. The tag is intentionally not moved; A6 is appended in keeping with the A1–A5 precedent because no threshold/metric/model/gate/headline binding is changed.*

### AMENDMENT A7 — Anchor correction + headline reframe to the protocol-gap decomposition

**Date:** 2026-06-11
**Status:** Human-ratified. Appended after `prereg-v1` (NOT re-tagged), consistent with the A1–A6 precedent.
**Trigger:** External review caught that §2's "verified factual ground" UQC anchor (0.89–0.97 on HP-KR)
contradicts its own cited source. Primary-source verification (this turn) confirms the error and
surfaces a stronger headline. This is a §0c STOP-and-surface event; the human is the ratifier.

---

#### A7.1 — Correction of a misimported "locked fact" (correctness fix, not preference)

**What was wrong.** §2 listed, as verified ground truth to be reproduced, "UQC reaches 0.89–0.97 on
HP-KR." This number is NOT in the parent paper and contradicts it.

**Primary-source verification (read end-to-end this turn):**
- **Liars' Bench (arXiv:2511.16035)** ran Pacchiardi's Unrelated Question Classifier (UQC) and reports,
  averaged across datasets, balanced accuracy 0.60 / AUROC near chance for UQC; and **specifically on
  HP-KR-type datasets, "balanced accuracies and AUROCs close to chance, while recall collapses toward
  either 0% or 100%."** Their protocol: train one classifier per base model on **80 questions uniformly
  sampled across eight OTHER training datasets**, then evaluate **zero-shot** on HP-KR (a deliberate
  out-of-distribution / detector-transfer protocol — detectors are not allowed dataset-specific
  fitting, simulating a deployment monitor facing a novel lie type).
- **Pacchiardi et al. (arXiv:2309.15840)** reports ~0.9–0.99 AUROC — but on **instructed factual lies, a
  different dataset that did NOT include HP-KR.** This is the number that was misimported as "HP-KR
  ground truth."
- Confirmed mechanism (Pacchiardi §5.5): the UQC reads **disposition**, not surface text — its score
  correlates r=0.84–0.96 with the probability the model will lie in subsequent responses. This supports
  the transcript-blind design: the signal is dispositional, not lexical.

**Corrected §2 anchor (replaces the 0.89–0.97 line verbatim):**
> Pacchiardi's original UQC reaches ~0.9–0.99 AUROC on instructed factual lies (a DIFFERENT dataset;
> does not include HP-KR). On HP-KR specifically, Liars' Bench's **zero-shot (OOD-transfer) UQC is
> ≈ chance** (arXiv:2511.16035; UQC averaged 0.60 balanced accuracy across datasets, near-chance on
> HP-KR). **No in-distribution UQC number on HP-KR is published.** Establishing one — and explaining
> its gap from the benchmark's OOD result — is THIS PROJECT'S CONTRIBUTION. There is therefore NO
> "target number to recover"; calibrating the implementation to hit 0.89–0.97 is explicitly forbidden,
> because that number is unachievable on HP-KR in the OOD protocol and would only be hit in-distribution
> via leakage. UQC implementation correctness is verified by the OOD reproduction (A7.3), not by
> matching a factual-lie anchor.

This reorients the project from "recover a known signal" to "explain a discrepancy."

#### A7.2 — Headline reframe: from legibility-correlation to protocol-gap decomposition

**Old spine (superseded):** "Is the internal deception direction externally legible? Do probe and UQC
agree?" — Agreement is the boring/expected outcome (a lie that leaves one trace leaves several), and
the in-distribution probe upper bound already implies it.

**New spine (the headline):** The parent paper got UQC ≈ chance on HP-KR (OOD protocol); this project
measures UQC ≈ 0.80 on HP-KR (in-distribution, within-template CV). **That gap is the result, and it is
a statement about PROTOCOL, not signal absence.**

> On transcript-blind self-knowledge lies (HP-KR), the deceptive signal is fully encoded and
> behaviorally separable IN-DISTRIBUTION — UQC ≈ 0.80, leakage-controlled. Liars' Bench's near-chance
> behavioral result on HP-KR is therefore a statement about **detector transfer across lie types**, not
> about whether the lie leaves a behavioral trace. The bottleneck for catching novel deception
> behaviorally is **generalization, not signal absence.** Direct self-report (Self-Eval) is the one
> channel that fails even in-distribution (max(SE-A,SE-B) ≈ 0.61) — the introspective-inaccessibility
> kicker, now a supporting line rather than the headline.

**Framing discipline (required, signals taste not naivety):** state explicitly that the benchmark's OOD
eval is the CORRECT proxy for deployment monitoring; this project does not claim the benchmark is wrong.
It claims their negative result means something more specific than the field's natural reading ("the lie
is hidden"). This is a meta-methodological contribution: how to interpret a "our detector failed on
dataset X" result.

**What is preserved unchanged:** probe, UQC, Self-Eval, the §3 CV scheme, the five guards, the
GREEN/AMBER/RED gate structure, T3 activations, the T5 behavioral apparatus, and §6 failure-mode-1
(no scope expansion — this is a re-pointing, not a rebuild). The introspection result already obtained
survives as the kicker.

#### A7.3 — NEW FIRST GATE (before T6/bridge): adjudicate the 0.80 (protocol vs leakage)

The entire reframe is a bet that the in-distribution 0.80 survives leakage scrutiny. Three explanations
must be separated BEFORE any downstream claim:

1. **Protocol** (in-distribution vs OOD) → the headline above.
2. **Leakage** (within-template CV lets persona/template signal bleed) → 0.80 collapses toward chance;
   we then CONFIRM Liars' Bench and land RED: "behavioral detection fails on HP-KR even in-distribution."
   Still publishable, not brilliant.
3. **OOD-robust implementation improvement** → stronger, but requires showing transfer; out of scope.

**Task T5.7 (NEW, GPU, the adjudicating gate):**
- (a) Reproduce the parent paper's **exact OOD protocol**: train UQC on instructed-factual-lie data
  (80 questions sampled across non-HP-KR datasets, per their method), evaluate **zero-shot** on HP-KR.
  **Expected ≈ chance.** This both reproduces their result AND validates our harness (if we DON'T get
  chance, our OOD pipeline is wrong, not theirs).
- (b) Our in-distribution within-template CV (already have: 0.80).
- (c) The leakage test (already partially done: confound check showed within-template AUROC 0.72–0.82,
  template-alone 0.53 — evidence AGAINST crude leakage; A7.3 tightens this with a stem-disjoint /
  leave-question-cluster check, not just template-stratified).
- **Headline holds ONLY IF** (a) reproduces ≈chance AND (c) confirms 0.80 is leakage-clean.
- If not → pivot to RED before the bridge work is wasted.

This makes T5.7 the true first gate. T6/T8/T9 do not run until T5.7 routes GREEN/AMBER.

#### A7.4 — Standing rule: primary-source verification of every §2 fact at lock time

One locked "fact" was wrong because it was elevated to ground truth without primary-source check.
NEW STANDING RULE: every number, anchor, and citation in §2 (and any that enters a writeup) must be
verified against its primary source AT LOCK TIME, with the source URL/arXiv-id recorded inline. Verified
this turn: 2511.16035 (Liars' Bench UQC≈chance on HP-KR), 2309.15840 (Pacchiardi disposition mechanism +
factual-lie anchor), 2510.01070 (Cywiński/Nanda/Conmy/Marks, secret elicitation, prefill attacks — the
turf-reality paper), 2604.13386 (Nordby et al., multi-layer probe ensembling, +78% on HP-KR, directions
rotate across layers — informs T6). Given prior README-fabrication history, this rule is non-negotiable.

#### A7.5 — T6 note (from the Nordby verification, separate from the reframe)
Nordby et al. (2604.13386) find single-layer probes are brittle on HP-KR and multi-layer ENSEMBLING gives
+78% AUROC, because deception directions rotate gradually across layers. T6's layer sweep should
therefore (a) report the per-layer curve (expected: no single sharp peak), and (b) include a multi-layer
ensemble probe as a registered variant, not only the single best layer. This is a design improvement to
T6, ratified here, independent of whether T5.7 routes the headline GREEN or RED.

# AMENDMENT A8 — T5.7a re-scope: OOD anchor recomputed from Liars' Bench released per-example artifacts

**Date:** 2026-06-11
**Status:** Human-ratified (operator exchange, 2026-06-11). Appended after `prereg-v1` (NOT re-tagged), consistent with the A1–A7 precedent.
**Trigger:** Discovery that the public `Cadenza-Labs/liars-bench` repo (commit `ba10de150873d53a34e88278346f857962f82de3`, verified identical on the pod clone) releases, for our exact model (`mistral-small-3.1-24b-instruct`) on the exact v2 HP-KR population (2358 rows, 697 lie / 1661 honest — byte-matching our T2 counts): per-example OOD detector scores (`pacchiardi_probability`, `pacchiardi_prediction`), the full 39-question elicitation prompts AND model responses, a 39-dim `binary_features` vector per example, the trained detector (`classifier.pkl`), and its training set (`training_data.csv`). This is a §0c surfaced decision; the human is the ratifier.

---

## A8.1 — Re-scope of A7.3(a): recompute, do not reproduce

A7.3(a) specified reproducing the parent paper's OOD protocol ourselves on the GPU (`t57_generate.py`, 65Q×1656). It is superseded by:

> **Recompute the OOD anchor directly from Liars' Bench's released per-example artifacts**, restricted to our 1656-row transcript-blind core via a validated join (A8.2). Operator's ratifying statement, recorded verbatim: *"A7.3(a) said reproduce; we instead recompute from their released artifacts, which is a stronger anchor for the identical reason — and we still validate our harness via the optional 39-Q run."*

Rationale: same examples, same model, their own scores — zero reproduction error, zero GPU. The in-distribution counterpart cell (A8.3) is likewise computable on CPU from their released `binary_features`, because the protocol axis (what the classifier trains on) is independent of the feature-generation step, which they have already executed end-to-end with their own pipeline.

**`t57_generate.py` is demoted to an OPTIONAL REGISTERED VARIANT** (A8.6), decided AFTER the CPU gate routes. T5.7a as re-scoped is entirely CPU.

## A8.2 — Join specification and STOP rule

The LB eval CSV carries a unique `index` column (contiguous 3543–5900) and `template_index` (values 1/5) inside `meta`. Join order:

1. **Index join (primary):** match `_orig_row_index` (from `artifacts/behavioral_scores.parquet`) to LB `index`, auto-detecting direct equality vs. a constant offset (+3543 if ours is 0-based within the HP-KR split).
2. **Content-hash join (fallback only):** whitespace-normalized hash of the full conversation on both sides (`hpkr_with_keys.parquet` text columns vs. parsed `messages`), keys required unique on both sides.
3. **Acceptance criterion (binding):** exactly 1656/1656 rows matched AND 100% `deceptive`-label agreement AND 100% template agreement AND joined class counts equal the A6-frozen 650 lie / 1006 honest. **Any failure → status BLOCKED, surface to human, no downstream number is computed.** A partially-validated join is a wrong join.

## A8.3 — Binding cells and the frozen routing rule

All cells computed on the joined 1656-row core, L2 logistic regression (C=1.0, sklearn default penalty), train-fold-only mean imputation of the 39-dim binary features (14/2358 full-set rows carry a NaN), seed=0, scores pooled out-of-fold:

- **OOD anchor (no training):** AUROC of their released `pacchiardi_probability` on the core; balanced accuracy of their released hard predictions (expected: all-honest collapse, 0.500 — their global `decision_threshold` 0.0769 vs. HP-KR score mass 0.0058 ± 0.0031, i.e., the published "≈chance" decomposes as calibration collapse + weak residual ranking).
- **In-distribution, their bank + their binary features:** (i) within-template stratified k-fold (k=5, A3 scheme, min-class-per-fold ≥ 20 asserted); (ii) **stem-disjoint GroupKFold over the 1030 T5.7b stems (the routing cell)**; (iii) cross-template A↔B consistency.
- **External Self-Eval anchor (no training):** AUROC of their released `prediction_extracted` on the core (classified per A8.5).

**Frozen routing rule (the single new threshold in this amendment, pre-committed before any core-restricted number exists; advisory full-set previews 0.687 vs 0.579 were known at freeze time and are recorded in the log, not in any artifact):**

> Let GAP = [stem-disjoint in-distribution AUROC, their bank + binary features, core] − [OOD anchor AUROC, core].
> **GAP ≥ 0.05 → "protocol/transfer" wording is retained in the headline.**
> **GAP < 0.05 → the headline leads with the readout/encoding factor** ("the trace is present but only a richer readout sees it — readout impoverishment, not transfer, is the bottleneck"). This is an alternate publishable landing, not a RED.
> **In ALL branches** the write-up reports the additive decomposition with BOTH factors named and quantified (protocol: OOD→in-distribution at fixed features; readout: binary→logprob encoding and 39Q→12Q bank), per the 2026-06-11 log commitment that the edge over Liars' Bench is not folded into a single-cause story. Leakage (A7.3 explanation 2) is already excluded four ways by T5.7b and is not re-litigated by this rule.

## A8.4 — Anchor corrections under the A7.4 standing rule (verified against released artifacts at `ba10de15`)

1. **Internal-probe anchor:** the previously noted "0.516 supervised LR" matches a `results/probes/results.csv` row whose population is **1569 honest / 1129 deceptive — a non-v2 dataset version**. It is FORBIDDEN as the anchor (same error class as the 0.89–0.97 misimport). The n-matched v2 anchors are: **mean probe AUROC 0.4632** (`results/probes/mean_probe_results.csv`) and **last-token probe AUROC 0.5386** (`results/probes/last_token_probe_results.csv`), both `train_dataset = repe_honesty__plain` — i.e., **their internal probes are also transfer/OOD probes**, symmetric with the behavioral story; our in-distribution T8 probe occupies an unpublished cell. The "0.66 diff-in-means" is UNPINNED: it enters nothing until its exact file is re-pulled on the pod and its population confirmed as 1661/697.
2. **OOD training-set description:** the released `training/mistral-small-3.1-24b-instruct/training_data.csv` contains **90 questions across 9 datasets** (10 each; all instructed lies, `model_lied=True`, paired lie/truth binary features). The A7/log phrase "80 questions across eight other datasets" does not match the artifact; the artifact governs, and the wording is reconciled against the paper text at T10.
3. **OOD anchor magnitude:** their full-set HP-KR number is **AUROC 0.5790** (recomputed from per-example `pacchiardi_probability`; matches their own `results/blackbox/pacchiardi.csv` value 0.579044 to 6 d.p.). Write-ups quote this exact figure with the calibration-collapse mechanism, not the bare word "chance."

## A8.5 — External Self-Eval anchor classification

Liars' Bench's self-eval protocol elicits a 0–100 lying score and explicitly permits reasoning before the verdict. It therefore maps to our **SE-C (CoT-permitted) axis**, NOT to the binding no-CoT `max(SE-A, SE-B)`. Their full-set AUROC is **0.5139** (`prediction_extracted`). It is reported alongside SE-C as an external anchor and is never compared 1:1 against our 0.606.

## A8.6 — Optional registered variant: the 39-Q harness run (≤1 GPU-hr cap)

Decided AFTER the A8.3 gate routes, in light of what it shows. If run: our harness generates their 39-Q bank over the core, recording binary AND logprob features. Dual purpose: (a) fills the missing 39Q-logprob quadrant of the attribution matrix; (b) restores A7.3(a)'s harness-validation function via feature-level agreement between our generated binaries and their released responses on identical examples. This is the ONLY remaining GPU work in T5.7.

## A8.7 — A5 and provenance constraints on third-party artifacts

The LB clone contains raw WMDP-derived transcripts (`messages`). Nothing from the clone enters our repo or artifacts except indices, hashes, labels, template ids, and metric values; LB text columns are dropped immediately after join-key construction. `classifier.pkl` is NOT unpickled (unnecessary — per-example probabilities are released — and unpickling third-party files is an execution risk). Every consumed LB file's sha256 and the repo commit SHA are recorded inside `artifacts/t57a_anchor.json`. Repo licensing for artifact reuse is checked once at T10.

**Scope:** binds T5.7a only. T5.7b (PASS), T6–T10, all guards, gates, thresholds, and the frozen prereg body are unchanged. `prereg-v1` remains at `2f5afb9`.


# AMENDMENT A8 — T5.7a re-scope: OOD anchor recomputed from Liars' Bench released per-example artifacts

**Date:** 2026-06-11
**Status:** Human-ratified (operator exchange, 2026-06-11). Appended after `prereg-v1` (NOT re-tagged), consistent with the A1–A7 precedent.
**Trigger:** Discovery that the public `Cadenza-Labs/liars-bench` repo (commit `ba10de150873d53a34e88278346f857962f82de3`, verified identical on the pod clone) releases, for our exact model (`mistral-small-3.1-24b-instruct`) on the exact v2 HP-KR population (2358 rows, 697 lie / 1661 honest — byte-matching our T2 counts): per-example OOD detector scores (`pacchiardi_probability`, `pacchiardi_prediction`), the full 39-question elicitation prompts AND model responses, a 39-dim `binary_features` vector per example, the trained detector (`classifier.pkl`), and its training set (`training_data.csv`). This is a §0c surfaced decision; the human is the ratifier.

---

## A8.1 — Re-scope of A7.3(a): recompute, do not reproduce

A7.3(a) specified reproducing the parent paper's OOD protocol ourselves on the GPU (`t57_generate.py`, 65Q×1656). It is superseded by:

> **Recompute the OOD anchor directly from Liars' Bench's released per-example artifacts**, restricted to our 1656-row transcript-blind core via a validated join (A8.2). Operator's ratifying statement, recorded verbatim: *"A7.3(a) said reproduce; we instead recompute from their released artifacts, which is a stronger anchor for the identical reason — and we still validate our harness via the optional 39-Q run."*

Rationale: same examples, same model, their own scores — zero reproduction error, zero GPU. The in-distribution counterpart cell (A8.3) is likewise computable on CPU from their released `binary_features`, because the protocol axis (what the classifier trains on) is independent of the feature-generation step, which they have already executed end-to-end with their own pipeline.

**`t57_generate.py` is demoted to an OPTIONAL REGISTERED VARIANT** (A8.6), decided AFTER the CPU gate routes. T5.7a as re-scoped is entirely CPU.

## A8.2 — Join specification and STOP rule

The LB eval CSV carries a unique `index` column (contiguous 3543–5900) and `template_index` (values 1/5) inside `meta`. Join order:

1. **Index join (primary):** match `_orig_row_index` (from `artifacts/behavioral_scores.parquet`) to LB `index`, auto-detecting direct equality vs. a constant offset (+3543 if ours is 0-based within the HP-KR split).
2. **Content-hash join (fallback only):** whitespace-normalized hash of the full conversation on both sides (`hpkr_with_keys.parquet` text columns vs. parsed `messages`), keys required unique on both sides.
3. **Acceptance criterion (binding):** exactly 1656/1656 rows matched AND 100% `deceptive`-label agreement AND 100% template agreement AND joined class counts equal the A6-frozen 650 lie / 1006 honest. **Any failure → status BLOCKED, surface to human, no downstream number is computed.** A partially-validated join is a wrong join.

## A8.3 — Binding cells and the frozen routing rule

All cells computed on the joined 1656-row core, L2 logistic regression (C=1.0, sklearn default penalty), train-fold-only mean imputation of the 39-dim binary features (14/2358 full-set rows carry a NaN), seed=0, scores pooled out-of-fold:

- **OOD anchor (no training):** AUROC of their released `pacchiardi_probability` on the core; balanced accuracy of their released hard predictions (expected: all-honest collapse, 0.500 — their global `decision_threshold` 0.0769 vs. HP-KR score mass 0.0058 ± 0.0031, i.e., the published "≈chance" decomposes as calibration collapse + weak residual ranking).
- **In-distribution, their bank + their binary features:** (i) within-template stratified k-fold (k=5, A3 scheme, min-class-per-fold ≥ 20 asserted); (ii) **stem-disjoint GroupKFold over the 1030 T5.7b stems (the routing cell)**; (iii) cross-template A↔B consistency.
- **External Self-Eval anchor (no training):** AUROC of their released `prediction_extracted` on the core (classified per A8.5).

**Frozen routing rule (the single new threshold in this amendment, pre-committed before any core-restricted number exists; advisory full-set previews 0.687 vs 0.579 were known at freeze time and are recorded in the log, not in any artifact):**

> Let GAP = [stem-disjoint in-distribution AUROC, their bank + binary features, core] − [OOD anchor AUROC, core].
> **GAP ≥ 0.05 → "protocol/transfer" wording is retained in the headline.**
> **GAP < 0.05 → the headline leads with the readout/encoding factor** ("the trace is present but only a richer readout sees it — readout impoverishment, not transfer, is the bottleneck"). This is an alternate publishable landing, not a RED.
> **In ALL branches** the write-up reports the additive decomposition with BOTH factors named and quantified (protocol: OOD→in-distribution at fixed features; readout: binary→logprob encoding and 39Q→12Q bank), per the 2026-06-11 log commitment that the edge over Liars' Bench is not folded into a single-cause story. Leakage (A7.3 explanation 2) is already excluded four ways by T5.7b and is not re-litigated by this rule.

## A8.4 — Anchor corrections under the A7.4 standing rule (verified against released artifacts at `ba10de15`)

1. **Internal-probe anchor:** the previously noted "0.516 supervised LR" matches a `results/probes/results.csv` row whose population is **1569 honest / 1129 deceptive — a non-v2 dataset version**. It is FORBIDDEN as the anchor (same error class as the 0.89–0.97 misimport). The n-matched v2 anchors are: **mean probe AUROC 0.4632** (`results/probes/mean_probe_results.csv`) and **last-token probe AUROC 0.5386** (`results/probes/last_token_probe_results.csv`), both `train_dataset = repe_honesty__plain` — i.e., **their internal probes are also transfer/OOD probes**, symmetric with the behavioral story; our in-distribution T8 probe occupies an unpublished cell. The "0.66 diff-in-means" is UNPINNED: it enters nothing until its exact file is re-pulled on the pod and its population confirmed as 1661/697.
2. **OOD training-set description:** the released `training/mistral-small-3.1-24b-instruct/training_data.csv` contains **90 questions across 9 datasets** (10 each; all instructed lies, `model_lied=True`, paired lie/truth binary features). The A7/log phrase "80 questions across eight other datasets" does not match the artifact; the artifact governs, and the wording is reconciled against the paper text at T10.
3. **OOD anchor magnitude:** their full-set HP-KR number is **AUROC 0.5790** (recomputed from per-example `pacchiardi_probability`; matches their own `results/blackbox/pacchiardi.csv` value 0.579044 to 6 d.p.). Write-ups quote this exact figure with the calibration-collapse mechanism, not the bare word "chance."

## A8.5 — External Self-Eval anchor classification

Liars' Bench's self-eval protocol elicits a 0–100 lying score and explicitly permits reasoning before the verdict. It therefore maps to our **SE-C (CoT-permitted) axis**, NOT to the binding no-CoT `max(SE-A, SE-B)`. Their full-set AUROC is **0.5139** (`prediction_extracted`). It is reported alongside SE-C as an external anchor and is never compared 1:1 against our 0.606.

## A8.6 — Optional registered variant: the 39-Q harness run (≤1 GPU-hr cap)

Decided AFTER the A8.3 gate routes, in light of what it shows. If run: our harness generates their 39-Q bank over the core, recording binary AND logprob features. Dual purpose: (a) fills the missing 39Q-logprob quadrant of the attribution matrix; (b) restores A7.3(a)'s harness-validation function via feature-level agreement between our generated binaries and their released responses on identical examples. This is the ONLY remaining GPU work in T5.7.

## A8.7 — A5 and provenance constraints on third-party artifacts

The LB clone contains raw WMDP-derived transcripts (`messages`). Nothing from the clone enters our repo or artifacts except indices, hashes, labels, template ids, and metric values; LB text columns are dropped immediately after join-key construction. `classifier.pkl` is NOT unpickled (unnecessary — per-example probabilities are released — and unpickling third-party files is an execution risk). Every consumed LB file's sha256 and the repo commit SHA are recorded inside `artifacts/t57a_anchor.json`. Repo licensing for artifact reuse is checked once at T10.

**Scope:** binds T5.7a only. T5.7b (PASS), T6–T10, all guards, gates, thresholds, and the frozen prereg body are unchanged. `prereg-v1` remains at `2f5afb9`.
